{
  "name": "core-spike",
  "private": true,
  "description": "PROTOTYPE — throwaway core spike for the v1-plan map, ticket 05. Not production.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --strictPort --host"
  },
  "dependencies": {
    "pixi.js": "^8.19.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^7.0.0"
  }
}
